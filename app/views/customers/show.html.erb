<div class="row">
  <div class="col-lg-12">
    <h1>顧客詳細</h1>
    <dl>
    <dt>お名前</dt>
    <dd style="margin:0px 0px 10px 20px"><%= @customer.full_name %></dd>
    <dt>電子メール</dt>
    <dd style="margin:0px 0px 10px 20px"><%= @customer.email %></dd>
    <dt>会社名</dt>
    <dd style="margin:0px 0px 10px 20px"><%= @customer.company.name %></dd>
    <dt>役職名</dt>
    <dd style="margin:0px 0px 10px 20px"><%= @customer.post.position_name %></dd>
    </dl>
      <%= link_to '編集', edit_customer_path(@customer), class: "btn btn-default" %>
      <%= link_to '戻る', customers_path, class: "btn btn-default" %>
    <hr />
    <h2>対応履歴</h2>
      <% @comments.each do |comment| %>
        <div class="media">
          <div class="media-body">
            <%= simple_format(comment.body) %>
            <p class="text-muted">
              投稿記事:<%= comment.created_at.strftime('%Y%m%d %H:%M') %>
              <%= link_to "削除する", comment_path(comment), method: :delete %>
            </p>
          </div>
        </div>
<<<<<<< HEAD
      </div>
    <% end %>
=======
      <% end %>
<!-- form_forを使用するとHTMLのフォーム(入力する項目)を生成します -->
<!-- reviewという変数は今回のcrmにはまったく関係ありません -->
<!-- CAMPのレジュメからのコピーはやめましょう！理解して書いて頂きたいです -->
>>>>>>> fd768e7ebd74e0a1f99021afef72ee859ba86772
    <%= form_for(@comment) do |f| %>
      <% if @comment.errors.any? %>
        <div id="error_explanation" class="alert alert-danger">
<<<<<<< HEAD
          <div><%= @comment.errors.count %> 件のエラーがあります。</div>
          <ul>
          <% @comment.errors.full_messages.each do |comment| %>
            <li><%= comment %></li>
          <% end %>
          </ul>
=======
<!-- commentという変数は存在しません -->                
          <div><%= @comment.errors.count %> 件のエラーがあります。
          </div>
            <ul>
<!-- commenという変数は存在しません -->
<!-- eachの行に「=」を付けてしまうとHTMLとして出力されてしまうので、「=」 -->
              <% @comment.errors.full_messages.each do |comment| %>
                <li><%= comment %></li>
              <% end %>
            </ul>
>>>>>>> fd768e7ebd74e0a1f99021afef72ee859ba86772
        </div>    
      <% end %>
    <% end %>
    <%= form_for(@comment, url: { controller: :comments, action: :create }) do |f| %>
      <div class="form-group">
        <%= f.label :body, '対応内容' %><br>
        <%= f.text_area :body, class: "form-control" %>
      </div>
        <%=f.hidden_field :customer_id, value: @customer.id %>
        <%= f.submit class: "btn btn-default" %>
    <% end %>
  </div>
</div>